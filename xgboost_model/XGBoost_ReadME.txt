XGBoost was computationally exhaustive. 
We used HPC computing to perform the work using BASH scripts that called the R script. 
The results of the computing are saved as a collection of models, tuning, confusion matrices, and plots.